global:
  scrape_interval: 15s # Cada cuánto tiempo va a buscar métricas

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Opcional si quieres monitorear tu propio backend o contenedor de Docker
  # - job_name: 'ecommerce-backend'
  #   static_configs:
  #     - targets: ['host.docker.internal:3000']